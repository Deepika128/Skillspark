{
  "name": "skillspark",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "build": "npm run build --workspace=frontend && (rm -rf dist || true) && mv frontend/dist ./dist",
    "dev": "npm run dev --workspace=frontend",
    "backend:dev": "node backend/index.js"
  },
  "engines": {
    "node": ">=20.x"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "body-parser": "^1.20.2"
  }
}
